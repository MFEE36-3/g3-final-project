import React from 'react';

export default function MemberActivity() {
  const activityStyle = {
    width: '100%',
    height: '400px',
    backgroundColor: '#fcc8a1',
    borderRadius: '20px',
    boxShadow: 'inset -5px -5px 5px rgba(123, 123, 123, 0.5)',
    marginTop: '50px',
  };
  return <div style={activityStyle}>輪播牆</div>;
}
